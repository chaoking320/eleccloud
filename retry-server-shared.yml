# Container runtime configuration for shared MySQL / Redis deployment
# Injected via spring.config.additional-location with higher priority than application.yml
#
# Requires shared infrastructure (infra/docker-compose.yml) and shared-network.
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/retry_platform?useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&rewriteBatchedStatements=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=GMT%2B8
    username: root
    password: 123456
  redis:
    host: redis
    port: 6379
    database: 1
    password: 123456
    redisson:
      config: |
        singleServerConfig:
          address: "redis://redis:6379"
          password: "123456"
          database: 2
          connectionPoolSize: 64
          connectionMinimumIdleSize: 10
